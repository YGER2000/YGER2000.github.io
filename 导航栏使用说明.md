# 现代化导航栏使用说明

## ✨ 已实现的功能

### 1. 顶部导航栏布局
- **左侧**：头像（可点击回到首页）
- **中间**：导航菜单（Home、Archives、About）
- **右侧**：搜索框 + 深浅色主题切换按钮

### 2. 响应式设计
- 桌面端：完整的横向导航栏
- 移动端：汉堡菜单，点击展开

### 3. 交互功能
- **主题切换**：点击太阳/月亮图标切换深浅色模式，设置会保存到浏览器
- **搜索功能**：输入关键词后按回车或点击搜索按钮，使用 Google 站内搜索
- **滚动效果**：滚动页面时导航栏会添加阴影效果
- **移动端菜单**：点击汉堡图标展开/收起菜单

## 🎨 如何自定义

### 修改头像

在 `themes/typing/_config.yml` 中修改：

```yaml
avatar: https://avatars.githubusercontent.com/u/YGER2000  # 你的头像 URL
```

**头像来源选项**：
1. **GitHub 头像**：`https://avatars.githubusercontent.com/u/你的用户名`
2. **本地图片**：将图片放到 `themes/typing/source/images/` 目录，然后使用 `/images/avatar.png`
3. **外部链接**：任何可访问的图片 URL

### 修改导航菜单

在 `themes/typing/_config.yml` 中修改：

```yaml
menu:
  首页: /
  博客: /archives
  关于: /about
  项目: /projects  # 可以添加更多菜单项
```

### 修改颜色主题

编辑 `themes/typing/source/css/modern-nav.css`，修改 CSS 变量：

```css
:root {
  --link-color: #3b82f6;  /* 链接颜色 */
  --hover-bg: #f3f4f6;    /* 悬停背景色 */
  /* 更多变量... */
}

body.dark {
  --link-color: #60a5fa;  /* 深色模式链接颜色 */
  /* 更多变量... */
}
```

### 修改搜索功能

编辑 `themes/typing/source/js/modern-nav.js`，找到 `performSearch` 函数：

```javascript
function performSearch() {
  const query = searchInput.value.trim();
  if (query) {
    // 可以改为其他搜索引擎或自定义搜索
    const siteUrl = window.location.hostname;
    window.open(`https://www.google.com/search?q=site:${siteUrl} ${query}`, '_blank');
  }
}
```

## 📁 相关文件

- **模板文件**：`themes/typing/layout/_partial/header.ejs`
- **样式文件**：`themes/typing/source/css/modern-nav.css`
- **脚本文件**：`themes/typing/source/js/modern-nav.js`
- **配置文件**：`themes/typing/_config.yml`

## 🚀 查看效果

1. 确保本地服务器正在运行：`npx hexo server`
2. 在浏览器打开：`http://localhost:4000`
3. 尝试以下操作：
   - 点击主题切换按钮
   - 在搜索框输入内容并搜索
   - 调整浏览器窗口大小查看响应式效果
   - 在移动端查看汉堡菜单

## 💡 提示

- 头像建议使用 **正方形** 图片，尺寸 200x200 以上
- 如果没有设置头像，会显示昵称的首字母作为占位符
- 主题切换的设置会保存在浏览器的 localStorage 中
- 搜索功能目前使用 Google 站内搜索，需要网站已被 Google 收录
