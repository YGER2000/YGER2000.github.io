# 1. 先在本地预览
npx hexo server

# 2. 在浏览器打开 http://localhost:4000 查看效果
# 3. 如果不满意，继续修改，保存后页面会自动刷新
# 4. 满意后，按 Ctrl+C 停止服务器

# 5. 部署到 GitHub
npx hexo clean && npx hexo generate && npx hexo deploy

# 6. 等待 1-2 分钟，访问 https://yger2000.github.io 查看线上效果


常用命令速查表
命令	简写	作用
npx hexo server	npx hexo s	启动本地服务器预览
npx hexo generate	npx hexo g	生成静态文件
npx hexo deploy	npx hexo d	部署到 GitHub
npx hexo clean	-	清理缓存
npx hexo new "文章标题"	npx hexo n "文章标题"	创建新文章
npx hexo generate --deploy	npx hexo g -d	生成并部署（一步完成）
